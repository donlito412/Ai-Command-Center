{
  "name": "ai-command-center-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "command-center"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=command-center",
    "build": "npm run build --workspace=command-center",
    "start": "npm run start --workspace=command-center"
  }
}
